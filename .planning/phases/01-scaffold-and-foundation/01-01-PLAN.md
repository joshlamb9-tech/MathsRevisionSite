---
phase: 01-scaffold-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nojekyll
  - index.html
  - assets/css/styles.css
  - foundation/index.html
  - core/index.html
  - additional/index.html
autonomous: false
requirements:
  - INFRA-01
  - INFRA-03
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-05

must_haves:
  truths:
    - "Pupil can open the GitHub Pages URL and see a styled homepage with Dosis font and Mowden Hall navy/gold colour scheme"
    - "Site displays without horizontal scrolling on a 375px-wide phone viewport"
    - "Jekyll processing is disabled — .nojekyll file is at repo root"
    - "Foundation, Core, and Additional level index pages exist and load without 404"
  artifacts:
    - path: ".nojekyll"
      provides: "Jekyll bypass"
      min_lines: 0
    - path: "index.html"
      provides: "Site homepage with inline nav"
      contains: "Dosis"
    - path: "assets/css/styles.css"
      provides: "Base stylesheet with design tokens, Dosis, Mowden colours, responsive layout"
      contains: "--mh-navy"
      min_lines: 80
    - path: "foundation/index.html"
      provides: "Foundation level index page"
    - path: "core/index.html"
      provides: "Core level index page"
    - path: "additional/index.html"
      provides: "Additional level index page"
  key_links:
    - from: "index.html"
      to: "assets/css/styles.css"
      via: "root-relative link tag"
      pattern: "href=\"/assets/css/styles.css\""
    - from: "foundation/index.html"
      to: "assets/css/styles.css"
      via: "root-relative link tag"
      pattern: "href=\"/assets/css/styles.css\""
---

<objective>
Create a working GitHub Pages site with Dosis font, Mowden Hall colours, mobile-responsive base CSS, .nojekyll, and level index pages for Foundation, Core, and Additional.

Purpose: Establishes the visual foundation and Jekyll bypass that all content phases depend on. Without .nojekyll, prime notation breaks on the live URL. Without mobile CSS, every content page will require individual fixes. Without the level indexes, there is no navigation skeleton for the 24 topic stubs that follow in 01-03.

Output: A deployed GitHub Pages site with a styled homepage, three level index pages, and a single base stylesheet. No content yet — structure and style only.
</objective>

<execution_context>
@/Users/josh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-foundation/01-RESEARCH.md

Key decisions already locked:
- Vanilla HTML/CSS/JS — no npm, no build step
- Dosis font via Google Fonts CDN (wght 300–800, display=swap)
- Mowden Hall colours: placeholder navy #1a2744, gold #c9962c (LOW confidence — Josh must confirm)
- Inline nav in every HTML file — NOT fetch() fragments (Safari/CORS bug on GitHub Pages)
- Root-relative asset paths (/assets/css/styles.css) — NOT relative (../assets/...) which break at depth
- GitHub Pages from main branch root, no GitHub Actions needed

French revision site reference: /Users/josh/marvin/y8-french-revision/
- styles-v2.css — CSS custom property patterns to follow
- index.html — inline nav and Dosis import pattern to follow
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo scaffold — .nojekyll, homepage, base stylesheet</name>
  <files>
    .nojekyll
    index.html
    assets/css/styles.css
  </files>
  <action>
Create three files in the project root (/Users/josh/projects/maths-revision/):

**1. `.nojekyll`** — empty file (zero bytes), placed at repo root. Disables Jekyll processing. This is the fix for Pitfall 3: without it, `f'(x)` prime notation breaks on the live GitHub Pages URL because Jekyll converts `'` to a curly apostrophe that KaTeX cannot parse.

**2. `assets/css/styles.css`** — base stylesheet. Create the `assets/css/` directory. The stylesheet must include:

CSS custom properties in `:root`:
```css
:root {
  --mh-navy:         #1a2744;   /* PLACEHOLDER — Josh to confirm */
  --mh-gold:         #c9962c;   /* PLACEHOLDER — Josh to confirm */
  --mh-navy-light:   #2a3a5c;
  --bg:              #f4f2ef;
  --white:           #ffffff;
  --text:            #18181b;
  --text-mid:        #3f3f46;
  --text-muted:      #71717a;
  --border:          #e4e4e7;
  --foundation-bg:   #fef3c7;
  --foundation-accent: #d97706;
  --core-bg:         #dbeafe;
  --core-accent:     #2563eb;
  --additional-bg:   #ede9fe;
  --additional-accent: #7c3aed;
}
```

Global reset and body:
- `box-sizing: border-box` on `*`
- `font-family: 'Dosis', sans-serif` on `body`
- `background: var(--bg)` on `body`
- `color: var(--text)` on `body`
- `overflow-x: hidden` on `body` (prevents horizontal scroll — DESIGN-05)
- `margin: 0; padding: 0` on `body`

Header / nav:
- `.site-header` — `background: var(--mh-navy); color: var(--white)`
- `.header-inner` — `max-width: 900px; margin: 0 auto; padding: 0.75rem 1rem; display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 0.5rem`
- `.brand-name` — `font-size: 1.2rem; font-weight: 700; color: var(--white)`
- `.brand-sub` — `font-size: 0.75rem; color: rgba(255,255,255,0.7)`
- `.header-nav` — `display: flex; gap: 1rem; flex-wrap: wrap`
- `.hn-link` — `color: var(--white); text-decoration: none; font-weight: 500; padding: 0.25rem 0.5rem; border-radius: 4px`
- `.hn-link:hover` — `background: var(--mh-navy-light)`
- `.hn-link.active` — `background: var(--mh-gold); color: var(--white)`

Main content:
- `.main-content` — `max-width: 900px; margin: 0 auto; padding: 1.5rem 1rem`

Hero / intro:
- `.hero` — `text-align: center; padding: 2rem 0 1.5rem`
- `.hero h1` — `font-size: 2rem; font-weight: 800; color: var(--mh-navy); margin-bottom: 0.5rem`
- `.hero p` — `color: var(--text-mid); font-size: 1.1rem`

Level cards (used on homepage):
- `.level-grid` — `display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; margin-top: 1.5rem`
- `.level-card` — `display: block; border-radius: 8px; padding: 1.25rem; text-decoration: none; transition: transform 0.15s; border: 1px solid var(--border)`
- `.level-card:hover` — `transform: translateY(-2px)`
- `.level-card.foundation` — `background: var(--foundation-bg); border-color: var(--foundation-accent)`
- `.level-card.core` — `background: var(--core-bg); border-color: var(--core-accent)`
- `.level-card.additional` — `background: var(--additional-bg); border-color: var(--additional-accent)`
- `.level-card h2` — `font-size: 1.2rem; font-weight: 700; margin: 0 0 0.25rem`
- `.level-card p` — `font-size: 0.9rem; color: var(--text-mid); margin: 0`

Topic list (used on level index pages):
- `.topic-list` — `list-style: none; padding: 0; margin: 1rem 0`
- `.topic-list li` — `margin: 0`
- `.topic-list a` — `display: block; padding: 0.6rem 0.75rem; border-radius: 6px; text-decoration: none; color: var(--text); border: 1px solid var(--border); margin-bottom: 0.5rem; font-weight: 500`
- `.topic-list a:hover` — `background: var(--white); border-color: var(--mh-navy)`

Breadcrumb:
- `.breadcrumb` — `font-size: 0.85rem; color: var(--text-muted); margin-bottom: 1rem`
- `.breadcrumb a` — `color: var(--text-mid); text-decoration: none`
- `.breadcrumb a:hover` — `text-decoration: underline`

Page title:
- `.page-title` — `font-size: 1.6rem; font-weight: 800; color: var(--mh-navy); margin: 0 0 0.25rem`
- `.level-badge` — `display: inline-block; font-size: 0.75rem; font-weight: 600; padding: 0.2rem 0.6rem; border-radius: 999px; text-transform: uppercase; letter-spacing: 0.05em`
- `.level-badge.foundation` — `background: var(--foundation-bg); color: var(--foundation-accent)`
- `.level-badge.core` — `background: var(--core-bg); color: var(--core-accent)`
- `.level-badge.additional` — `background: var(--additional-bg); color: var(--additional-accent)`

Footer:
- `footer` — `text-align: center; padding: 2rem 1rem; color: var(--text-muted); font-size: 0.85rem; border-top: 1px solid var(--border); margin-top: 2rem`

Responsive (max-width: 600px):
- `.hero h1` — `font-size: 1.5rem`
- `.level-grid` — `grid-template-columns: 1fr`
- `.header-inner` — reduce padding

**3. `index.html`** — site homepage. Must include:
- `<!DOCTYPE html>` declaration
- `<html lang="en">`
- `<head>` with:
  - `<meta charset="UTF-8">`
  - `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
  - `<title>Maths Revision | Mowden Hall Year 8 CE</title>`
  - Google Fonts preconnect links and Dosis import (wght 300;400;500;600;700;800, display=swap)
  - `<link rel="stylesheet" href="/assets/css/styles.css">`
- Inline nav (NOT fetch — Safari CORS bug). Use this exact structure:
```html
<header class="site-header">
  <div class="header-inner">
    <a href="/" class="header-brand" style="display:flex;align-items:center;gap:0.75rem;text-decoration:none">
      <div class="brand-name">Maths Revision</div>
    </a>
    <nav class="header-nav">
      <a href="/" class="hn-link active">Home</a>
      <a href="/foundation/" class="hn-link">Foundation</a>
      <a href="/core/" class="hn-link">Core</a>
      <a href="/additional/" class="hn-link">Additional</a>
    </nav>
  </div>
</header>
```
- Main content with `.hero` section (h1: "Year 8 CE Maths Revision", p: "Choose your level below to start revising")
- Three `.level-card` links to `/foundation/`, `/core/`, `/additional/` with descriptive text for each level
- Footer: "Mowden Hall School · Year 8 CE Maths Revision"

Do NOT include KaTeX in index.html — no equations on the homepage. KaTeX is added in Plan 01-02.
  </action>
  <verify>
    <automated>
      cd /Users/josh/projects/maths-revision &&
      test -f .nojekyll && echo "PASS: .nojekyll exists" || echo "FAIL: .nojekyll missing" &&
      test -f index.html && echo "PASS: index.html exists" || echo "FAIL: index.html missing" &&
      test -f assets/css/styles.css && echo "PASS: styles.css exists" || echo "FAIL: styles.css missing" &&
      grep -q "mh-navy" assets/css/styles.css && echo "PASS: colour tokens present" || echo "FAIL: colour tokens missing" &&
      grep -q "Dosis" index.html && echo "PASS: Dosis font in index.html" || echo "FAIL: Dosis missing" &&
      grep -q "overflow-x: hidden" assets/css/styles.css && echo "PASS: overflow-x hidden on body" || echo "FAIL: overflow-x missing" &&
      grep -q "hn-link" index.html && echo "PASS: inline nav present" || echo "FAIL: inline nav missing"
    </automated>
    <manual>Optional: python3 -m http.server 8080 from project root, then visit http://localhost:8080 — should see navy header, Dosis font, three level cards</manual>
  </verify>
  <done>
    .nojekyll exists at repo root (zero bytes).
    assets/css/styles.css contains --mh-navy, --mh-gold, Dosis font stack, overflow-x: hidden on body, responsive breakpoint at 600px.
    index.html loads with Dosis font, inline nav linking to /foundation/ /core/ /additional/, three level cards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Level index pages (Foundation, Core, Additional)</name>
  <files>
    foundation/index.html
    core/index.html
    additional/index.html
  </files>
  <action>
Create three level index pages. Each must be a complete, self-contained HTML file with inline nav.

**Pattern for all three pages:**
- Full `<!DOCTYPE html>` structure
- Same `<head>` as index.html (Dosis font, /assets/css/styles.css) — NO KaTeX yet (added in 01-02)
- Inline nav — same as index.html but with the correct `.hn-link.active` on the current level link
- `.main-content` wrapper
- Breadcrumb: `<p class="breadcrumb"><a href="/">Home</a> › [Level Name]</p>`
- `.page-title` h1 with level name
- `.level-badge` showing the level name (foundation/core/additional class)
- `.topic-list` `<ul>` with `<li><a href="/level/slug/">Topic Name</a></li>` for every topic at that level — these are stubs created in 01-03, so the links should be present but the pages they point to won't exist yet
- Brief intro paragraph describing what the level covers

**foundation/index.html** — active link: Foundation. Topics (6):
- `/foundation/fractions/` — Fractions
- `/foundation/division/` — Division
- `/foundation/prime-factors/` — Prime Factors
- `/foundation/estimation/` — Estimation
- `/foundation/long-multiplication/` — Long Multiplication
- `/foundation/averages/` — Averages
Intro: "Foundation topics are Paper 1 (non-calculator). These form the core arithmetic skills tested at CE."

**core/index.html** — active link: Core. Topics (15):
- `/core/ratio/` — Ratio
- `/core/percentages/` — Percentages
- `/core/algebra-basics/` — Algebra Basics
- `/core/shape/` — Shape
- `/core/averages/` — Averages
- `/core/angles/` — Angles
- `/core/probability/` — Probability
- `/core/coordinates-graphs/` — Coordinates and Graphs
- `/core/transformations/` — Transformations
- `/core/volume-surface-area/` — Volume and Surface Area
- `/core/sequences-nth-term/` — Sequences and nth Term
- `/core/straight-line-graphs/` — Straight Line Graphs
- `/core/speed-distance-time/` — Speed, Distance and Time
- `/core/powers-roots/` — Powers and Roots
- `/core/charts-data/` — Charts and Data Interpretation
Intro: "Core topics cover the main CE Maths paper. Mix of calculator and non-calculator questions."

**additional/index.html** — active link: Additional. Topics (3):
- `/additional/index-laws/` — Index Laws
- `/additional/expanding-factorising/` — Expanding and Factorising
- `/additional/pythagoras/` — Pythagoras
Intro: "Additional topics are for the higher CE paper. Requires solid Core knowledge first."

All three files must use root-relative paths for assets — `/assets/css/styles.css` not `../assets/css/styles.css`.
  </action>
  <verify>
    <automated>
      cd /Users/josh/projects/maths-revision &&
      test -f foundation/index.html && echo "PASS: foundation/index.html" || echo "FAIL: foundation/index.html missing" &&
      test -f core/index.html && echo "PASS: core/index.html" || echo "FAIL: core/index.html missing" &&
      test -f additional/index.html && echo "PASS: additional/index.html" || echo "FAIL: additional/index.html missing" &&
      grep -c "topic-list" foundation/index.html | grep -q "^[1-9]" && echo "PASS: foundation has topic list" || echo "FAIL: foundation topic list missing" &&
      grep -q "prime-factors" foundation/index.html && echo "PASS: prime-factors link in foundation" || echo "FAIL: prime-factors missing" &&
      grep -q "coordinates-graphs" core/index.html && echo "PASS: coordinates-graphs in core" || echo "FAIL: missing" &&
      grep -q "pythagoras" additional/index.html && echo "PASS: pythagoras in additional" || echo "FAIL: missing" &&
      grep -q "/assets/css/styles.css" core/index.html && echo "PASS: root-relative path in core" || echo "FAIL: relative path bug"
    </automated>
    <manual>Optional: verify all 6 Foundation topics, 15 Core topics, 3 Additional topics appear in their respective index pages</manual>
  </verify>
  <done>
    foundation/index.html lists all 6 Foundation topics as links.
    core/index.html lists all 15 Core topics as links.
    additional/index.html lists all 3 Additional topics as links.
    All three pages use root-relative paths for CSS.
    Breadcrumb "Home > [Level]" present on each page.
    Total: 24 topic links locked across the three index pages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify local site appearance and colour scheme</name>
  <files>none — human verification only</files>
  <action>Human verifies the site renders correctly in a browser before proceeding to 01-02.</action>
  <what-built>
    Styled homepage, base stylesheet with Dosis + Mowden colours, .nojekyll, and three level index pages with all 24 topic links. The site exists only locally — GitHub Pages deployment and KaTeX come in 01-02.
  </what-built>
  <how-to-verify>
    1. Run: `cd /Users/josh/projects/maths-revision && python3 -m http.server 8080`
    2. Visit http://localhost:8080 — should see: navy header, "Maths Revision · Mowden Hall Year 8 CE" branding, Dosis font, three level cards (yellow/blue/purple tones)
    3. Click Foundation — should show 6 topics listed, breadcrumb "Home › Foundation"
    4. Click Core — should show 15 topics listed
    5. Click Additional — should show 3 topics listed
    6. Resize browser to ~375px width — no horizontal scrollbar, layout stacks cleanly
    7. Confirm Mowden Hall colours look right, OR note what needs adjusting (colours are LOW confidence placeholders — #1a2744 navy, #c9962c gold)
  </how-to-verify>
  <verify>Human confirms site renders correctly per the how-to-verify steps above.</verify>
  <done>Josh types "approved" or provides colour correction notes.</done>
  <resume-signal>Type "approved" to continue to 01-02 (KaTeX), or describe any issues with the layout/colours</resume-signal>
</task>

</tasks>

<verification>
- .nojekyll present at repo root (zero bytes)
- assets/css/styles.css contains --mh-navy, --mh-gold, Dosis font-family, overflow-x: hidden on body, responsive CSS at 600px breakpoint
- index.html has inline nav with links to /foundation/, /core/, /additional/ using root-relative paths
- foundation/index.html, core/index.html, additional/index.html all exist
- All 24 topic slugs appear as links across the three index pages: 6 Foundation + 15 Core + 3 Additional
- No fetch() calls anywhere — inline nav only
- No relative asset paths (../assets) — root-relative (/assets) only
</verification>

<success_criteria>
- Pupil can open http://localhost:8080 and see a styled homepage with Dosis font
- Mowden Hall navy/gold colour scheme applied (placeholder values — Josh to confirm)
- Three level index pages accessible, each showing the correct topic list
- No horizontal scrolling at 375px viewport width
- .nojekyll at repo root ready for the GitHub Pages push in 01-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-and-foundation/01-01-SUMMARY.md` with:
- Files created and their purpose
- CSS patterns established (tokens, class names)
- Confirmation of inline nav pattern used
- Note on placeholder colour values (Josh must confirm --mh-navy and --mh-gold)
- Any issues encountered
</output>
